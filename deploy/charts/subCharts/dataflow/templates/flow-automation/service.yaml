{{- $global := default dict .Values.global -}}
{{- $flowAutomation := mergeOverwrite (deepCopy (default dict .Values.flowAutomation)) (default dict $global.flowAutomation) -}}
{{- if .Values.components.flowAutomation.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dataflow.flowAutomation.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dataflow.labels" . | nindent 4 }}
    app.kubernetes.io/name: {{ include "dataflow.flowAutomation.fullname" . }}
spec:
  {{- if $flowAutomation.service.enableDualStack }}
  ipFamilies:
  - IPv6
  - IPv4
  ipFamilyPolicy: PreferDualStack
  {{- end }}
  type: {{ $flowAutomation.service.type }}
  ports:
    - port: {{ $flowAutomation.service.port }}
      targetPort: dataflow-pub
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/instance: {{ .Release.Name }}

---

apiVersion: v1
kind: Service
metadata:
  name: {{ include "dataflow.flowAutomation.fullname" . }}-private
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dataflow.labels" . | nindent 4 }}
    app.kubernetes.io/name: {{ include "dataflow.flowAutomation.fullname" . }}-private
spec:
  {{- if $flowAutomation.service.enableDualStack }}
  ipFamilies:
  - IPv6
  - IPv4
  ipFamilyPolicy: PreferDualStack
  {{- end }}
  type: {{ $flowAutomation.service.type }}
  ports:
    - port: {{ $flowAutomation.service.port }}
      targetPort: dataflow-priv
      protocol: TCP
      name: private
  selector:
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
