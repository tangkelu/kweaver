{{- $env := include "mergedGlobalValues.env" . | fromYaml -}}
{{- $depServices := include "mergedGlobalValues.depServices" . | fromYaml -}}
{{- if semverCompare ">=1.16-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: apps/v1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Deployment
metadata:
  name: isfwebthrift
  namespace: {{ .Release.Namespace }}
  annotations:
    checksum/configmap: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
  labels:
    app: {{ .Chart.Name }}
spec:
  replicas: {{ include "mergedGlobalValues.replicaCount" . }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Chart.Name }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  strategy:
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Chart.Name }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      securityContext:
        runAsUser: 5000
        runAsGroup: 5000
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app.kubernetes.io/name
                  operator: In
                  values:
                  - {{ .Chart.Name }}
              topologyKey: "kubernetes.io/hostname"
      containers:
        # isfwebthrift
        - name: {{ .Chart.Name }}
          image: {{ include "mergedGlobalValues.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            readOnlyRootFilesystem: true
          ports:
            - name: svc-port
              containerPort: {{ .Values.service.containerPort }}
          env:
            - name: PUBLIC_API_BASE
              value: "{{ $env.publicProtocol}}://{{ $depServices.eacp.publicHttpHost}}:{{ $depServices.eacp.publicHttpPort}}"
            - name: PRIVATE_API_BASE
              value: "{{ $env.privateProtocol}}://{{ $depServices.eacp.privateHttpHost}}:{{ $depServices.eacp.privateHttpPort}}"
            - name: HYDRA_PUBLIC_URI
              value: "{{.Values.service.oauth2.hydraPublicProtocol}}://{{ $depServices.hydra.publicHost}}:{{ $depServices.hydra.publicPort}}"
            - name: HOST_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
          envFrom:
            - secretRef:
                name: {{ .Chart.Name }}
          readinessProbe:
            httpGet:
              path: /
              port: {{ .Values.service.containerPort }}
            initialDelaySeconds: {{ .Values.service.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.service.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.service.readinessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.service.readinessProbe.failureThreshold }}
          livenessProbe:
            httpGet:
              path: /
              port: {{ .Values.service.containerPort }}
            initialDelaySeconds: {{ .Values.service.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.service.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.service.livenessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.service.livenessProbe.failureThreshold }}
          volumeMounts:
            - name: {{ .Chart.Name }}-conf
              mountPath: /config
            - name: conf-path
              mountPath: /sysvol/conf
            - mountPath: /var/cache/nginx
              name: nginx-cache
            - mountPath: /var/run
              name: nginx-run
            - mountPath: /sysvol/tmp
              name: sysvol-tmp
          resources:
            requests:
              cpu: {{ .Values.resources.requests.cpu }}
              memory: {{ .Values.resources.requests.memory }}
      dnsConfig:
        options:
          - name: single-request-reopen
          - name: use-vc
      volumes:
        - name: {{ .Chart.Name }}-conf
          configMap:
            name: {{ .Chart.Name }}-conf
        - name: conf-path
          hostPath:
            path: /sysvol/conf
        - name: nginx-cache
          emptyDir: {}
        - name: nginx-run
          emptyDir: {}
        - name: sysvol-tmp
          emptyDir: {}
