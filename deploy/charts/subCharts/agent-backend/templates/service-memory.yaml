# Agent Memory Service (内部服务)
apiVersion: v1
kind: Service
metadata:
  name: agent-backend-memory
  namespace: {{ .Release.Namespace }}
  labels:
    app: agent-backend-factory
    service: agent-backend-memory
spec:
  ports:
    - name: memory-port
      protocol: TCP
      port: {{ .Values.agentMemory.port }}
      targetPort: {{ .Values.agentMemory.port }}
  selector:
    app: agent-backend-factory
  type: ClusterIP
