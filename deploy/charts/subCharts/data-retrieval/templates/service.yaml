apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Chart.Name }}
spec:
  ports:
    - name: api-port
      port: {{ .Values.ports.apiPort }}
      protocol: TCP
      targetPort: {{ .Values.ports.apiPort }}
  selector:
    app: {{ .Chart.Name }}
