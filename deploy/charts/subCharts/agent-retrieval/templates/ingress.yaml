{{- if .Values.service.agentRetrieval.ingress.enabled -}}
{{ if .Values.service.agentRetrieval.ingress.interface }}
{{- $depServices := include "mergedGlobalValues.depServices" . | fromYaml -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.moduleName }}-ingress
  namespace: {{ .Release.Namespace }}
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "500M"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
{{- if index $depServices "class-443" "ingressClass" }}
  ingressClassName: {{ index $depServices "class-443" "ingressClass" }}
{{- end }}
  rules:
  - http:
      paths:
        {{- range .Values.service.agentRetrieval.ingress.interface.path }}
        - path: {{ . }}
          pathType: Prefix
          backend:
            service:
              name: {{ $.Values.moduleName }}
              port:
                number: {{ $.Values.service.port }}
        {{- end }}
{{ end }}
{{- end }}
